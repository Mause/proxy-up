openapi: 3.1.0
info:
  title: proxy-up
  version: 0.0.1

security:
  - AuthHeader: []

servers:
  - url: "https://up.vc.mause.me"

components:
  securitySchemes:
    AuthHeader:
      scheme: header
      type: http
paths: {}
